language: php

php:
  - 7.1

before_script:
  - cp .env.testing .env
  - touch database/database.sqlite
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit

notifications:
  email: false
